import { IsAvailablePipe } from './is-available.pipe';

describe('IsAvailablePipe', () => {
  it('create an instance', () => {
    const pipe = new IsAvailablePipe();
    expect(pipe).toBeTruthy();
  });
});
